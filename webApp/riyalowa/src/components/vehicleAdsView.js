import React, { Component } from 'react';
import { connect } from 'react-redux';
import { getPublishedVehicleAds } from '../redux/actions/vehicleAdActions';

class vehicleAdsView extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default connect(null,null)(vehicleAdsView);
